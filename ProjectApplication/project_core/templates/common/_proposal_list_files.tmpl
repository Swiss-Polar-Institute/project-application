{% if call_part.files %}
    <ul>
    {% for file in call_part.files %}
        <li><a href="{{ file.download_link }}"><i class="fas fa-download"></i> {{ file.name }}</a>: {{ file.description }}</li>
    {% endfor %}
    </ul>
{% endif %}
