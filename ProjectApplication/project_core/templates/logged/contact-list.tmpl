{% extends 'logged/_base_with_menus.tmpl' %}
{% block subtitle %}Contact list{% endblock %}

{% block contents %}
    <h1>Contacts</h1>
    {% if contacts %}
        <table class="table table-stripped table-sm table-hover display">
            <thead>
            <tr>
                <th>First name</th>
                <th>Surname</th>
                <th>Email</th>
                <th>Actions</th>
            </tr>
            </thead>

            <tbody>
            {% for contact in contacts %}
                <tr>
                    <td>{{ contact.person.first_name }}</td>
                    <td>{{ contact.person.surname }}</td>
                    <td>{% include 'common/_email_link_or_dash.tmpl' with email=contact.main_email %}
                    <td>{#<a class="btn btn-primary btn-xs"#}
                           {#href="{% url 'contact-update' pk=contact.id %}">Edit</a>#}
                        <a class="btn btn-primary btn-xs"
                           href="{% url 'contact-detail' pk=contact.id %}">View</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% else %}
        No contacts in the system
    {% endif %}
    <script type="text/javascript" class="init">
        $(document).ready(function () {
            $('table.display').DataTable({
                    "pageLength": 100
                }
            );
        });
    </script>
{% endblock %}
